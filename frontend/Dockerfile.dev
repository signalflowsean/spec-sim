# USE NODE AS A BASE IMAGE WITH AN ALPINE LINUX DISTRUBUTION
FROM node:alpine

# CREATE A WORKING DIRECTORY CALLED APP
WORKDIR '/frontend'

# COPY FILE THAT LISTS DEPENDENCIES
COPY package.json .

# INSTALL DEPENDENCIES
RUN npm install

# COPYING TWICE TO TAKE ADVANTAGE OF LAYER CACHING
COPY . . 

CMD ["npm", "run", "start"]